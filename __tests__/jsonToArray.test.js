const makeJsonArray = require("../script/jsonToArray");

describe("makeJsonArray", () => {
  test.todo("returns an array");
  test.todo(
    "takes an array with a an object with 1 key value pair and gives it back as an array in an array"
  );
  test.todo(
    "takes an array of two objects with 1 key value pair and gives it back as two arrays in an array"
  );
  test.todo(
    "takes an array of a single object with multiple key value pairs and returns an array"
  );
  test.todo("takes multiple objects with multiple values");
});
